using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public class animation_ordenations : MonoBehaviour
{
    List<GameObject> circuitos = new List<GameObject>();

    // Start is called before the first frame update
    void Start()
    {
        //GameObject oi = Instantiate(,Vector3.zero,Quaternion.identity); 

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
